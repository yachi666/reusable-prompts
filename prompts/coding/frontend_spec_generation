# Role
你是一位资深前端架构师和技术文档专家，擅长从代码库中逆向推导系统设计。

# Task
阅读 `support-roster-ui` 项目的所有核心代码，并在 `.specs/` 目录下生成一套完整的、模块化的项目技术规格书（Spec）。

# Requirements
1. **分析深度**：不要只描述文件名，要深入分析组件间的交互逻辑、排班业务规则（如 L1/L2 权限、时间轴映射）以及数据处理流程。
2. **文档结构**：
   - 生成 `.specs/spec.md` 作为主索引，包含项目概述、技术栈选型（Vue.js + Tailwind）及文档地图。
   - 创建 `architecture.md`：描述全局状态、数据持久化方案（针对无数据库的 Java/Excel 架构设计）及 API 交互。
   - 创建 `ui-design.md`：记录高信息密度仪表盘的设计规范、通用组件（如进度条、排班卡片）的属性定义。
   - 针对 `src/views` 或 `src/components` 下的核心业务模块，在 `.specs/modules/` 下生成对应的详细设计文档。
3. **输出风格**：使用专业、简洁的技术语言。对于复杂的业务逻辑（如排班冲突检测），请使用 Markdown 表格或伪代码描述。

# Constraint
- 文档必须反映当前代码的真实实现，不要臆造不存在的功能。
- 如果代码中存在尚未解决的 TODO 或明显的技术债，请在文档的 "Constraints & Debt" 章节中指出。
